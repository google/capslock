Capslock is an experimental tool for static analysis of Go packages. Share feedback and file bugs at github.com/google/capslock.
To get machine-readable full analysis output, use -output=json

Analyzed packages:{{range $val := .ModuleInfo}}
{{$val.Path}} {{$val.Version}}{{end}}
{{if .CapabilityStats}}{{range $index, $p := .CapabilityStats}}
{{$p.Capability}}: {{$p.Count}} references
{{$p.DirectCount}} direct, {{$p.TransitiveCount}} transitive
Example callpath: {{$p.ExampleCallpath}}
{{end}}
{{else}}Capslock found no capabilities in this package.{{end}}
